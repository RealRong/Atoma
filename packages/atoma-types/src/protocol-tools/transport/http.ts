export const HTTP_PATH_OPS = '/ops'
export const HTTP_PATH_SYNC_SUBSCRIBE = '/sync/subscribe'

export const http = {
    paths: {
        OPS: HTTP_PATH_OPS,
        SYNC_SUBSCRIBE: HTTP_PATH_SYNC_SUBSCRIBE
    }
} as const
